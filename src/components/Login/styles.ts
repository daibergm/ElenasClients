import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'row',
    alignItems: 'center',
  },
  scroll: {
    width: '100%',
    paddingHorizontal: 15,
  },
  subContainer: {
    flex: 1,
  },
  buttonContainer: {
    marginTop: 30,
    paddingBottom: 15,
  },
});
