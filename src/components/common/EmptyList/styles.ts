import { StyleSheet } from 'react-native';

import { Colors } from '../../../assets';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  title: {
    marginTop: 20,
    fontSize: 15,
    fontWeight: '600',
  },
  description: {
    marginTop: 12,
    fontSize: 15,
    color: Colors.grey,
  },
});
